spring: 
   datasource:
      username: team_store
      password: team_store
      url: jdbc:mysql://localhost:3306/team_store
      
   jpa:
      hibernate:
         ddl-auto: create
      show-sql: true
      defer-datasource-initialization: true
      
   sql: 
      init:
         mode: always